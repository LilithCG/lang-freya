@top Program { expression* }

@skip { space | LineComment }

expression {
  keyword |
  String |
  Application { "{" expression* "}" }
}

@tokens {
  keyword {"component"}

  String { '"' (!["\\] | "\\" _)* '"' }

  LineComment { "#" ![\n]* }

  space { $[ \t\n\r]+ }

  "{" "}"
}

@detectDelim
